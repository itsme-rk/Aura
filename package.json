{
  "name": "aura-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "mobile": "pnpm --filter mobile",
    "web": "pnpm --filter web",
    "clean": "pnpm -r exec rm -rf node_modules .expo dist build && rm -rf node_modules"
  }
}
